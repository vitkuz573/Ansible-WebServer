---
- name: Install prerequisites (deb packages)
  apt:
    update_cache: yes
    pkg:
      - sudo
      - wget
      - pwgen
      - unzip
      - python3-apt
      - python3-pip
      - python3-passlib

- name: Install prerequisites (python3 packages)
  pip:
    name: setuptools
    executable: pip3
