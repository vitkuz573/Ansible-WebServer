<VirtualHost *:{{ apache_port }}>
    ServerName {{ http_host }}
    ServerAlias www.{{ http_host }}
    DocumentRoot /var/www/{{ http_host }}/html
    <Directory /var/www/{{ http_host }}/html>
        AllowOverride All
    </Directory>
</VirtualHost>